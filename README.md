
# Project Run — Backend бегового трекера (REST API)

Backend-приложение для бегового трекера на Django + Django REST Framework.  
Позволяет атлетам записывать забеги, передавать GPS-координаты, участвовать в челленджах и взаимодействовать с тренерами.

## 🚀 Возможности

- **Пользователи**: атлеты и тренеры
- **Создание и управление забегами**
- **Статусы забега**: `init`, `in_progress`, `finished`
- **Приём и хранение GPS-координат во время забега (Position)**
- **Автоматический подсчёт дистанции, времени и скорости**
- **Челленджи**:
  - «Сделай 10 забегов»
  - «Пробеги 50 километров»
  - «2 километра за 10 минут»
- **Подписка атлетов на тренеров**
- **Рейтинг тренеров**
- **Аналитика для тренеров**
- **Пагинация, фильтрация, сортировка**
- **Автоматические тесты и CI (GitHub Actions)**

## 🧱 Архитектура (ключевые сущности)

- **User** — базовый пользователь
- **Athlete / Coach** — роли пользователя
- **Run** — забег атлета
- **Position** — GPS-точки забега
- **Challenge** — система достижений
- **Subscription / Rating** — подписки и оценки тренеров

## 🛠 Используемые технологии

- Python 3.12
- Django
- Django REST Framework
- SQLite (локально)
- PostgreSQL (Docker / production)
- Poetry
- Pytest + pytest-django
- Ruff, Black
- Docker, Docker Compose
- GitHub Actions (CI)

## ⚠️ Важно про Poetry и PyCharm

Если PyCharm сообщает:  
**Poetry executable is not found**, это означает, что:

- Poetry не установлен
- или PyCharm не знает путь к `poetry.exe`

Убедитесь, что Poetry установлен и путь к `poetry.exe` указан в настройках интерпретатора PyCharm.

## 📦 Установка и запуск (локально)

1️⃣ **Клонирование репозитория**:
```bash
git clone <REPO_URL>
cd project_run
```

2️⃣ **Установка зависимостей**:
```bash
poetry install
```

🔐 **Переменные окружения**  
Проект использует переменные окружения. Перед первым запуском скопируйте файл `.env.example` в `.env`:
```bash
cp .env.example .env
```

Минимально необходимое содержимое `.env` для локальной разработки:
```ini
DJANGO_SECRET_KEY=dev-secret-key
DEBUG=True
```
⚠️ Без `DJANGO_SECRET_KEY` проект не запустится (ошибка `RuntimeError: DJANGO_SECRET_KEY is not set`).

### ⚙️ Запуск проекта (локально)

Проект использует несколько конфигураций Django settings. Для локальной разработки применяется:

`config.settings.local`

3️⃣ **Применить миграции**:
```bash
poetry run python manage.py migrate --settings=config.settings.local
```

4️⃣ **Создать суперпользователя (для админки)**:
```bash
poetry run python manage.py createsuperuser --settings=config.settings.local
```

5️⃣ **Запустить сервер**:
```bash
poetry run python manage.py runserver --settings=config.settings.local
```

🌐 **Доступы**:
- **API**: [http://127.0.0.1:8000/api/](http://127.0.0.1:8000/api/)
- **Django Admin**: [http://127.0.0.1:8000/admin/](http://127.0.0.1:8000/admin/)

## 🐳 Запуск через Docker

1. Запуск проекта с Docker:
```bash
docker compose up --build
```

2. Применение миграций:
```bash
docker compose exec web poetry run python manage.py migrate --settings=config.settings.local
```

3. Создание суперпользователя:
```bash
docker compose exec web poetry run python manage.py createsuperuser --settings=config.settings.local
```

Сервер будет доступен по адресу:  
[http://127.0.0.1:8000/api/](http://127.0.0.1:8000/)

## 🧪 Тесты

**Локальный запуск тестов**:
```bash
poetry run pytest
```

**Проверяется**:
- основные API-эндпойнты
- жизненный цикл забега
- работа координат (Position)
- корректность API-ответов

## 🤖 CI (GitHub Actions)

В проекте настроен CI:

- ruff
- pytest

**Workflow файл**: `.github/workflows/ci.yml`  
Запускается при каждом push и pull request.

## 📡 Основные API эндпойнты

- **Company / Meta**  
  `GET /api/company_details/`

- **Users**  
  `GET /api/users/`

- **Runs**  
  `GET /api/runs/`  
  `POST /api/runs/{id}/start/`  
  `POST /api/runs/{id}/stop/`

- **Positions**  
  `GET /api/positions/?run={id}`

- **Challenges**  
  `GET /api/challenges/`

- **Coaches**  
  `POST /api/subscribe_to_coach/{coach_id}/`  
  `POST /api/rate_coach/{coach_id}/`  
  `GET /api/analytics_for_coach/{coach_id}/`

## 👤 Автор

**Denis Tarasov**  
GitHub: [https://github.com/Denchickus](https://github.com/Denchickus)
```

